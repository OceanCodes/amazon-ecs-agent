machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  override:
      - make release

test:
  override:
    - echo "no tests"

deployment:
  docker-image:
    branch: master
    commands:
      - docker tag amazon/amazon-ecs-agent:latest 524950183868.dkr.ecr.us-east-1.amazonaws.com/codeocean/amazon-ecs-agent:latest
      - `aws ecr get-login --region us-east-1`
      - docker push 524950183868.dkr.ecr.us-east-1.amazonaws.com/codeocean/amazon-ecs-agent:latest
